<!-- Comment -->
<div class="media">
    <a class="pull-left" href="#">
        <img class="media-object" src="<%= comment.author.avatar %>" alt="">
    </a>
    <div class="media-body">
        <h4 class="media-heading"><%= comment.author.fullname %>
            <small><%= comment.timestamp %></small>
        </h4>
        <%= comment.content %>
    </div>
</div>